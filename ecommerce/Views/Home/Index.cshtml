@model HomeViewModel

<partial name="_Carousel" />

<h2 class="mt-4">Products of the week</h2>
<h4 class="mb-4">Our best selling products!</h4>

<div class="row">
  @foreach (var product in Model.ProductsOfTheWeek)
  {
    <partial name="_ProductCard" model="product" />
  }
</div>